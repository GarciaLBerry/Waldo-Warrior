# Waldo-Warrior
Searches file paths locally stored on Windows in O(1) time. Will save your file data locally between uses. Search for a file by naming its exact file name to recieve the exact file path. Update the tree with /reconfig.
