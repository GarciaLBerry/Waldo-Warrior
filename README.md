# Waldo-Warrior
Searches file paths locally stored on Windows in O(1) time. Will save your file data locally between uses. Search for a file by naming its exact file path and update the tree with /reconfig.
